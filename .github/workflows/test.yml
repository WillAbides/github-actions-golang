on: [push, pull_request]
name: Test
jobs:
  test:
    strategy:
      matrix:
        go-version: [1.14.x, 1.15.x]
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - name: Install Go
      uses: actions/setup-go@v2
      with:
        go-version: ${{ matrix.go-version }}
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Test
      run: go test ./...

  test-cache:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            gobuild_path: ~/.cache/go-build
          - os: macos-latest
            gobuild_path: ~/Library/Caches/go-build
          - os: windows-latest
            gobuild_path: '%LocalAppData%\go-build'
    runs-on: ${{ matrix.os }}
    steps:
    - name: Install Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15.x
    - name: Checkout code
      uses: actions/checkout@v2
    - uses: actions/cache@v2
      env:
        module_download_cache: ~/go/pkg/mod               # Module download cache
        build_cache:           ${{ matrix.gobuild_path }} # Build cache
      with:
        path: |
          ${{ env.module_download_cache }}
          ${{ env.build_cache }}
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-
    - name: Test
      run: go test ./...
